server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        # SPA: serve index.html when the file isn’t found
        try_files $uri /index.html;
    }
}
